import * as React from 'react';

interface Props {
  variant: 'small' | 'medium' | 'large';
}
const UserInputButton: React.FC<Props> = ({ variant }) => {
  return <div></div>;
};

export default UserInputButton;
